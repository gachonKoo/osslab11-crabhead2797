import math

def pythagoras(a, b):
    # 직각삼각형 빗변 길이 계산: c = sqrt(a**2 + b**2)
    c = math.sqrt(a**2 + b**2)
    return c

def circle(r):
    # 원의 넓이 계산: Area = pi * r**2
    area = math.pi * r**2
    return area
